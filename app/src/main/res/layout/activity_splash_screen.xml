<?xml version="1.0" encoding="utf-8"?>
<!--
    Splash screen layout.
    ID convention: splash_{WIDGET}_{descriptor}
    LottieAnimationView ID: splash_LOTTIE_lottie (matches SplashScreenActivity binding ref).
    autoPlay="false" — animation is started manually in startAnimation() per L08 pattern.
    loop="false" — plays once then triggers onAnimationEnd → navigateNext().
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/splash_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/hq_background">

    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/splash_LOTTIE_lottie"
        android:layout_width="260dp"
        android:layout_height="260dp"
        app:lottie_rawRes="@raw/splash_animation"
        app:lottie_autoPlay="false"
        app:lottie_loop="false"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/splash_LBL_appName"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_chainStyle="packed" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/splash_LBL_appName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_md"
        android:text="@string/app_name"
        android:textAppearance="?attr/textAppearanceDisplaySmall"
        android:textColor="@color/hq_purple_primary"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/splash_LOTTIE_lottie"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
