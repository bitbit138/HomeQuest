# Firebase Setup

Copy `google-services.json.example` to `google-services.json` and fill in the values from [Firebase Console](https://console.firebase.google.com/) (Project Settings → Your apps → Download google-services.json).
